# environment for local Spark cluster
#
# start the cluster: docker-compose up&
# stop the cluster:  docker-compose down
# add workers:       docker-compose scale worker=2
#
COMPOSE_PROJECT_NAME=spark
COMPOSE_FILE=sandbox.yaml
