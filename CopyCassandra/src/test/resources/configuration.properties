# Runtime access to test properties
jmxPort=${cassandra.jmxPort}
rpcPort=${cassandra.rpcPort}
stopPort=${cassandra.stopPort}
storagePort=${cassandra.storagePort}
nativeTransportPort=${cassandra.nativeTransportPort}